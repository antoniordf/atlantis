<div class="container">
  <div class="row">
    <ul>
      <% current_user.bookings.each do |booking| %>
        <li><%= link_to "#{booking.yacht.name}", booking_path(booking) %></li>
      <% end %>
    </ul>
  </div>
</div>
